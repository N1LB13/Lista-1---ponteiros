#include <iostream>

int main()
{
    int mat[4], *p, x;

    /*
      p = mat + 1; // é a referencia do segundo elemento do vetor
      
      p = mat++ é um expressão inválida, pois não se pode adiiciona mais
      espaços a um vetor após a sua declaração.
      
      p = ++mat; mesmo caso do anterior.

      x = (*mat)++; é válido, a expressão "(*mat)" retorna o valor do primeiro elemento
      do array. O operador "++" é aplicado ao primeiro elemento do array e o valor de x será atualizado.
      

    */

    return 0;
}
